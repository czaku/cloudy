# T-005 — Lane Collision Detection

## Goal

Use product and lane metadata to prevent obviously unsafe parallel runs.

## Acceptance Criteria

- Cloudy warns or blocks when a conflicting run is already active in the same product+lane
- shared lane remains blocked by default
- override path is explicit, not silent
